function Anatomy(part, description, functions) constructor {
    self.part = part;                // e.g., "tentacles"
    self.description = description;  // e.g., "Long, flexible appendages"
    self.functions = functions;        // e.g., "used for grasping and caressing"
}