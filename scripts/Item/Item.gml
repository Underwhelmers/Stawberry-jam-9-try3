function Item(name, description, uses) constructor {
    self.name = name;                // e.g., "Aromatic Oil"
    self.description = description;  // e.g., "A bottle of fragrant oil."
    self.uses = uses;                // e.g., ["apply"]
}