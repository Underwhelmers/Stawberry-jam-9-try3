#macro DELTA_SECONDS (delta_time/1000000)
#macro SCREEN_W (global.camera.width)
#macro SCREEN_WIDTH (global.camera.width)

#macro SCREEN_H (global.camera.height)
#macro SCREEN_HEIGHT (global.camera.height)