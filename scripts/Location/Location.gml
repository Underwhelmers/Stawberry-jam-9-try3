function Location(name, description, connections) constructor {
    self.name = name;                // e.g., "Bed"
    self.description = description;  // e.g., "A large, comfortable bed draped in velvet."
    self.connections = connections;  // Array of connected Location structs
}